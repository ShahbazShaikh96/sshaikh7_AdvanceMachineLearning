# IMDB Text Classification: Learned Embeddings vs GloVe

This assignment compares two approaches for sentiment classification on the IMDB movie review dataset:

- a task-specific embedding layer learned from the training data
- a pretrained GloVe embedding layer used under limited-data conditions

The notebook evaluates how model performance changes as the number of labeled training samples increases.
